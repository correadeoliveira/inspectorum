import { ProgressDashboard } from "@/components/progress-dashboard";

export default function DashboardPage() {
  // A página agora simplesmente renderiza o componente, sem divs extras.
  return (
    <ProgressDashboard />
  );
}